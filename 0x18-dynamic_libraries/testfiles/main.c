#include <stdio.h>
#include "main.h"
/**
 * main - Tests if the created Dynamic library works properly.
 *
 * Return: 0 if successfull, 1 otherwise.
 */
int main(void)
{
	printf("%d\n", _strlen("Fudji Prince BOKANDE"));

	return (0);
}
