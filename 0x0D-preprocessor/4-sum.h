#ifndef SUM_H
#define SUM_H
#include <stdio.h>

/**
 * SUM(x, y) - macro that computes the sum of numbers
 *	x and y.
 * @x: parameter for first number
 * @y: parameter for second number
 */

#define SUM(x, y) ((x) + (y))

#endif /* SUM_H */
