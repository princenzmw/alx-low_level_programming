#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
#include <stdio.h>

/**
 * ABS(x) - macro that computes absolute value of x
 * @x: parameter to pass
 */

#define ABS(x) (((x) < 0) ? -(x) : (x))

#endif /* FUNCTION_LIKE_MACRO_H */
